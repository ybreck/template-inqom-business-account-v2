import React from 'react';

// Icon definitions have been removed. Please use src/constants/icons.tsx for shared icons.
// Mock data (mockClientInvoices, mockConversations) and utility functions 
// (getPDPStatusBadgeStyle, formatMessageTimestamp) have been removed as they are 
// now located in their respective feature modules (e.g., src/modules/clientInvoices/data.ts, 
// src/modules/conversations/data.ts & utils.ts).

// This file is now clean of duplicated icons and module-specific data/utils.
// If any TRULY global, app-wide, non-UI constants are needed, they can be placed here.
// For now, it's effectively empty.